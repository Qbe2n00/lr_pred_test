[tool.poetry]
name = "pulso-test"
version = "0.1.0"
description = "test for pulso"
authors = ["Qbe2n00 <hyunbeenthequeen@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
